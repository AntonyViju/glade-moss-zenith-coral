[project]
name = "aurea_theme"
authors = [
    { name = "Aurea", email = "hello@aurea.desk" }
]
description = "Premium Odoo-inspired desk theme for ERPNext"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.flit.module]
name = "aurea_theme"
